// The Evergreen Team Builder runs entirely client-side; shareable ?t= links
// are decoded in the browser against the bundled card database.
export const prerender = false;
export const ssr = false;
